---
title: Service Manifest Reference
description: This reference now lives on the main Deploy Services with MeshAgent page.
---

The service manifest reference now lives on [Deploy Services with MeshAgent](./packaging).

Jump to:

- [`container.environment`](./packaging#container-environment)
- [`container.storage`](./packaging#container-storage)
- [`ports.endpoints`](./packaging#ports-endpoints)
- [`variables`](./packaging#variables-servicetemplate-only)
- [`annotations`](./packaging#annotations)

<a id="container-environment"></a>
`container.environment` is documented on [Deploy Services with MeshAgent](./packaging#container-environment).

<a id="container-storage"></a>
`container.storage` is documented on [Deploy Services with MeshAgent](./packaging#container-storage).

<a id="ports-endpoints"></a>
`ports.endpoints` is documented on [Deploy Services with MeshAgent](./packaging#ports-endpoints).

<a id="variables-servicetemplate-only"></a>
`variables` is documented on [Deploy Services with MeshAgent](./packaging#variables-servicetemplate-only).

<a id="annotations"></a>
`annotations` are documented on [Deploy Services with MeshAgent](./packaging#annotations).
