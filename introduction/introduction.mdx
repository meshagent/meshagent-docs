---
title: "Overview"
---

# MeshAgent

[MeshAgent](https://www.meshagent.com) is the platform for building, deploying, and operating **room-based agent systems**.

A **room** is the runtime and collaboration boundary where humans, agents, tools, services, and shared state work together. Instead of treating every agent interaction as an isolated run, MeshAgent gives you a live workspace with built-in infrastructure, deployment surfaces, and product interfaces around that room.

Most agent products give you one part of the stack: model access, a sandbox, an orchestration loop, or a chat surface. MeshAgent gives you the full operating surface for collaborative agent applications: rooms, agents, built-in runtime APIs, deployable services, developer tooling, end-user interfaces, permissions, and observability.

If you want agents that can do real work with people, data, documents, queues, mail, tools, and custom code without stitching together your own runtime, that is what MeshAgent is for.

Every MeshAgent account includes both [MeshAgent Studio](../interfaces/meshagent_studio) and [Powerboards](../interfaces/powerboards). They connect to the same underlying projects and rooms, so developers can inspect and operate a room in Studio while end users work with that same room in Powerboards.

## Why teams use MeshAgent

- **Rooms as the center of the system**: Every room has shared context, participants, built-in APIs, and runtime behavior in one place.
- **Built-in infrastructure**: Storage, database, queues, sync/documents, containers, secrets, messaging, and developer events are already part of the room runtime.
- **Agents that can do more than chat**: Run agents across chat, mail, queues, toolkit calls, and other room-connected workflows.
- **One platform from local testing to production**: Use the CLI and MeshAgent Studio to iterate quickly, then deploy the same capabilities as project or room services.
- **Developer and end-user surfaces**: Developers build and inspect in [MeshAgent Studio](../interfaces/meshagent_studio). End users can work with agents in [Powerboards](../interfaces/powerboards), and both surfaces can reach the same rooms.
- **Fast start or full customization**: Start with built-in agents in Powerboards or MeshAgent Studio, or bring and build your own agents, tools, and services.
- **Governance and observability built in**: Control access, scope permissions, inspect logs and traces, and understand usage and cost without bolting on a separate control plane.

## The core idea

A **project** groups related rooms, members, services, credentials, interfaces, billing, and project-managed resources.

A **room** is where runtime work actually happens.

When a room becomes active, MeshAgent starts a session for that room, brings up the room runtime, and starts the services that should run there. When activity stops, the room shuts down automatically. Data written through room APIs such as storage, database, queues, and sync remains available according to that API's behavior.

Inside a room, people and agents can:

- chat and collaborate in real time
- work over shared files, data, and documents
- send work through queues and scheduled tasks
- use built-in and custom tools
- access containers, secrets, and other room infrastructure
- connect custom services and application logic

That gives you one place to coordinate runtime behavior, shared context, and collaboration instead of rebuilding those pieces around each individual agent.

## What MeshAgent lets you build

- internal copilots, operators, and research agents that work with teams over shared context
- customer-facing agents you can ship in Powerboards or your own UI
- document-heavy workflows where agents and people need to read, write, and coordinate over the same data
- multi-agent systems that combine chat, background queues, mail, toolkit calls, and scheduled work
- custom applications where agents need real runtime capabilities, not just prompt wrappers

## What you get out of the box

- **[MeshAgent CLI](../reference/meshagent_cli_help)** for local development, room operations, deployment, and automation from the terminal
- **[SDKs](../reference/sdk_reference)** for Python, TypeScript/JavaScript, Dart/Flutter, and .NET when you want to build custom services or application interfaces
- **[Room APIs](../room_api/overview)** for messaging, storage, database, queues, sync/documents, containers, secrets, and developer events
- **[Agents and tools](../agents/overview)** that can use built-in capabilities, custom toolkits, skills, and multiple model providers
- **[Services and deployment](../services_room_containers/overview)** so you can package agents, tools, connectors, and supporting code as project or room services
- **MeshAgent Server** as the managed backend that provisions rooms, runs services, scales runtime infrastructure, and tracks project usage
- **[MeshAgent Studio](../interfaces/meshagent_studio)** for development, testing, debugging, deployment, and project visibility
- **[Powerboards](../interfaces/powerboards)**, included with every MeshAgent account, for end-user interaction with built-in agents, deployed agents, and installable experiences
- **[Observability](../observability/intro)**, scoped access, and cost controls for production usage

## A quick taste

You can get from zero to a room-connected agent with just a few CLI commands:

```bash
meshagent setup
meshagent process join \
  --room myroom \
  --agent-name assistant \
  --channel chat \
  --model gpt-5.4 \
  --web-search \
  --require-storage \
  --require-document-authoring \
  --rule "You are a helpful research assistant. Answer clearly, use web search when needed, and save useful notes to room storage."
```

From there you can work with that agent in MeshAgent Studio, open the same room in Powerboards, add more tools and rules, expose more channels such as mail or queues, and deploy it as a managed service.

## Next steps

- [**Quickstart**](./cli_quickstart): Install MeshAgent, connect the CLI, run your first agent, and deploy it.
- [**Key Concepts**](./key_concepts): Learn how Rooms, Agents, Tools, and Services fit together.
- [**Architecture**](./architecture): Understand how projects, rooms, services, and interfaces fit together.
- [**Rooms**](../room_api/overview): Explore the room runtime and built-in APIs.
- [**Agents & Tools**](../agents/overview): Learn how agents work in MeshAgent.
