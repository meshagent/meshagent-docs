## Overview

The `MessagingClient` is the Room API for real-time participant-to-participant communication. Use it to send direct messages, broadcast updates, receive live events, and open streams for larger or incremental payloads.

## CLI commands

Start with the CLI help, then use a few common commands:

```bash bash
meshagent room messaging --help
meshagent room messaging list --room myroom
meshagent room messaging send --room myroom --to-participant-id PARTICIPANT_ID --type chat --data '{"text":"Hello"}'
meshagent room messaging broadcast --room myroom --data '{"content":"Hello everyone"}'
```

## Why use the Messaging API?

- Build chat, agent-human collaboration, and live status updates inside a room.
- Send typed JSON payloads instead of inventing a custom messaging protocol.
- Broadcast announcements to everyone in a room or send point-to-point messages to one participant.
- Stream larger payloads or partial updates when a single message is not enough.

## How it works

Messaging is participant-based. Once enabled, the client tracks remote participants and emits events for messages and participant changes. Use direct messages for one-to-one delivery, broadcasts for room-wide updates, and streams when the payload arrives in chunks or over time.

## Core concepts

### Participant model

Messaging treats every room member as a `Participant`, whether that participant is a human, an agent, or another service. That gives you one delivery model for direct messages, broadcasts, and stream setup instead of separate APIs for different participant types.

### Typed JSON messages

Each message has a `type` plus a JSON payload. That makes it easy to build simple chat flows, structured commands, workflow events, or agent-to-agent coordination on top of the same messaging surface.

### Optional attachments

Messages can also carry binary attachments. Use this when you need to send raw bytes along with the JSON payload, for example for images, voice snippets, or other generated artifacts.

### Streams for larger payloads

For large or incremental payloads, use streams instead of a single message. A stream opens between two participants, sends `MessageStreamChunk` values over time, and closes when the sender is done.

### Event-driven behavior

Once messaging is enabled, the client can emit events for incoming messages, participant changes, and accepted streams. That makes it suitable for real-time workflows where your application reacts immediately when another participant joins, sends a message, or starts a stream.

## Permissions and grants

The Messaging API is controlled by the `messaging` grant on the participant token.

In practice:

- `list` controls participant discovery
- `send` controls direct messages
- `broadcast` controls room-wide messages

See [API Scopes](../rest_api/api_scopes) and [Packaging and Deploying Services](../services/deployment/deploy_services).

## API reference

Use the methods below to enable room messaging, exchange direct or broadcast messages, and work with message streams.

### enable(onStreamAccept?)

Enables the messaging subsystem on the server. Optionally provides a callback that is triggered whenever an incoming stream is opened by a remote participant.


<CodeGroup>
```python Python
await room.messaging.enable(
    lambda reader: print(f"New stream opened: {reader.header}"))

```
```javascript NodeJs
await room.messaging.enable((reader) => {
  console.log("New stream opened:", reader._streamId);
});

```
```typescript TypeScript
await room.messaging.enable((reader) => {
  console.log("New stream opened:", reader._streamId);
});

```
```dart Dart
await room.messaging.enable((reader) {
  print("New stream opened: ${reader.header}");
});

```
```dotnet C#
await room.Messaging.Enable(onStreamAccept: reader =>
{
  Console.WriteLine($"Stream accepted: {reader.Header["name"]}");
});

```
</CodeGroup>



### disable()

Disables the messaging subsystem on the server.

<CodeGroup>
```python Python
await room.messaging.disable()

```
```javascript NodeJs
await room.messaging.disable();

```
```typescript TypeScript
await room.messaging.disable();

```
```dart Dart
await room.messaging.disable();

```
```dotnet C#
await room.Messaging.Disable();

```
</CodeGroup>



### sendMessage(to, type, message, attachment?)

Sends a message to a specific participant, optionally including a binary `attachment`.

**Parameters**:
- `to` – The participant to receive the message.
- `type` – A string that categorizes or labels the message.
- `message` – A JSON-serializable object containing message data.
- `attachment` – (Optional) A `Uint8Array` of binary data to attach.


<CodeGroup>
```bash CLI
meshagent room messaging send \
  --room myroom \
  --to-participant-id PARTICIPANT_ID \
  --type chat \
  --data '{"text":"Hey!"}'

```
```python Python
await room.messaging.send_message(
  to=my_participant,
  type="chat",
  message={"text": "Hey!"},
  attachment=b"\x01\x02\x03",
)

```
```javascript NodeJs
await room.messaging.sendMessage({
  "to": myParticipant,
  "type": "chat",
  "message": { text: "Hey!" },
  "attachment": new Uint8Array([1, 2, 3]),
});

```
```typescript TypeScript
await room.messaging.sendMessage({
  "to": myParticipant,
  "type": "chat",
  "message": { text: "Hey!" },
  "attachment": new Uint8Array([1, 2, 3]),
});

```
```dart Dart
await room.messaging.sendMessage(
  to: myParticipant,
  type: "chat",
  message: {"text": "Hey!"},
  attachment: Uint8List.fromList([1, 2, 3]),
);

```
```dotnet C#
await room.Messaging.SendMessage(
  to: myParticipant,
  type: "chat",
  message: new Dictionary<string, object?> { ["text"] = "Hey!" },
  attachment: new byte[] { 1, 2, 3 }
);

```
</CodeGroup>



### broadcastMessage(type, message, attachment?)

Broadcasts a message to **all** participants in the room.

**Parameters**:
- `type` – A string that categorizes or labels the message.
- `message` – A JSON-serializable object containing message data.
- `attachment` – (Optional) A `Uint8Array` of binary data to attach.

<CodeGroup>
```bash CLI
meshagent room messaging broadcast \
  --room myroom \
  --data '{"title":"Server Maintenance","content":"We will be down tonight."}'

```
```python Python
await room.messaging.broadcast_message(
  type="announcement",
  message={
    "title": "Server Maintenance",
    "content": "We will be down tonight.",
  },
)

```
```javascript NodeJs
await room.messaging.broadcastMessage({
  "type": "announcement",
  "message": {
    "title": "Server Maintenance",
    "content": "We will be down tonight.",
  }
});

```
```typescript TypeScript
await room.messaging.broadcastMessage({
  "type": "announcement",
  "message": {
    "title": "Server Maintenance",
    "content": "We will be down tonight.",
  }
});

```
```dart Dart
await room.messaging.broadcastMessage(
  type: "announcement",
  message: {
    "title": "Server Maintenance",
    "content": "We will be down tonight.",
  },
);

```
```dotnet C#
await room.Messaging.BroadcastMessage(
  type: "announcement",
  message: new Dictionary<string, object?> { 
   ["title"] = "Server Maintenance", 
   ["content"] = "We will be down tonight." 
});

```
</CodeGroup>



### createStream(to, header)

Creates a new stream directed at a single participant. Returns a `MessageStreamWriter` once the remote participant accepts the stream.

**Parameters**:
- `to` – The participant to receive the stream.
- `header` – A JSON-serializable object describing the stream.

**Returns**  
A `Promise<MessageStreamWriter>` that resolves once the remote participant accepts the stream.


<CodeGroup>
```python Python
writer = await room.messaging.create_stream(
  to=my_participant,
  header={"purpose": "video-stream"}
)

```
```javascript NodeJs
const writer = await room.messaging.createStream({
  to: myParticipant,
  header: {"purpose": "video-stream" },
});

```
```typescript TypeScript
const writer = await room.messaging.createStream({
  to: myParticipant,
  header: {"purpose": "video-stream" },
});

```
```dart Dart
final writer = await room.messaging.createStream(
  to: myParticipant,
  header: {"purpose": "video-stream"},
);

```
```dotnet C#
var writer = await room.Messaging.CreateStream(
  to: myParticipant,
  header: new Dictionary<string, object?> { ["name"] = "video-stream" }
);

```
</CodeGroup>



### remoteParticipants

Provides an iterable collection of all known remote participants. 

<CodeGroup>
```python Python
for participant in room.messaging.remote_participants:
    print(f"Participant: {participant.id}, role: {participant.role}")

```
```javascript NodeJs
for (const participant of room.messaging.remoteParticipants) {
  console.log(`Participant: ${participant.id}, role: ${participant.role}`);
}

```
```typescript TypeScript
for (const participant of room.messaging.remoteParticipants) {
  console.log(`Participant: ${participant.id}, role: ${participant.role}`);
}

```
```dart Dart
for (final participant in room.messaging.remoteParticipants) {
  print("Participant: ${participant.id}, role: ${participant.role}");
}

```
```dotnet C#
foreach (var participant in room.Messaging.RemoteParticipants)
{
  Console.WriteLine($"Participant: {participant.Id}, role: {participant.Role}");
}

```
</CodeGroup>



### MessageStreamChunk

Represents a chunk of data in a stream, consisting of a header and optional binary data.



### MessageStreamWriter

A writer for sending streaming data to a remote participant, created via `MessagingClient.createStream()`.

- **`write(MessageStreamChunk)`**  
  Sends a chunk of data in the ongoing stream.
  
- **`close(): Promise<void>`**  
  Closes the stream, signaling to the remote participant that no more chunks will be sent.

<CodeGroup>
```python Python
# Example usage after acquiring a MessageStreamWriter
await stream_writer.write(MessageStreamChunk(
  header={"part": 1},
  data=b"some binary data"
))
await stream_writer.close()

```
```javascript NodeJs
// Example usage after acquiring a MessageStreamWriter
await streamWriter.write(new MessageStreamChunk({
  header: { part: 1 },
  data: new Uint8Array([1, 2, 3])
}));
await streamWriter.close();

```
```typescript TypeScript
// Example usage after acquiring a MessageStreamWriter
await streamWriter.write(new MessageStreamChunk({
  header: { part: 1 },
  data: new Uint8Array([1, 2, 3])
}));
await streamWriter.close();

```
```dart Dart
// Example usage after acquiring a MessageStreamWriter
streamWriter.write(MessageStreamChunk(
  header: {"part": 1},
  data: Uint8List.fromList([1, 2, 3])
));
streamWriter.close();

```
```dotnet C#
// Send a chunk and close the stream
await streamWriter.Write(new MessageStreamChunk(
  header: new Dictionary<string, object?> { ["part"] = 1 },
  data: new byte[] { 1, 2, 3 }
));
await streamWriter.Close();

```
</CodeGroup>





## Additional Notes

1. **Event emission**
   Messaging events are exposed a little differently by language. Python uses `room.messaging.on(...)`, JavaScript/TypeScript inherit `EventEmitter`, Dart surfaces room events through `room.events`, and .NET uses `room.Messaging.On(...)`. In each case you can observe incoming messages and participant changes:

<CodeGroup>
```python Python
room.messaging.on(
    "message",
    lambda message: print("Received a message of type:", message.type))

room.messaging.on(
    "participant_added",
    lambda participant: print("A new participant joined:", participant.id))

```
```javascript NodeJs
room.messaging.on("message", ({ message }) => {
   console.log("Received a message of type:", message.type);
});

room.messaging.on("participant_added", (evt) => {
   console.log("A new participant joined:", evt.message.message);
});

```
```typescript TypeScript
room.messaging.on("message", ({ message }) => {
   console.log("Received a message of type:", message.type);
});

room.messaging.on("participant_added", (evt) => {
   console.log("A new participant joined:", evt.message.message);
});

```
```dart Dart
room.events.listen((event) {
   if (event is RoomMessageEvent) {
      print("Received a message of type: ${event.message.type}");
   }
});

```
```dotnet C#
room.Messaging.On("participant_added", data =>
{
  Console.WriteLine($"A new participant joined: {data["message"]}");
});

```
</CodeGroup>


2. **Remote Participants**  
   `MessagingClient` automatically keeps track of participants joining or leaving when the messaging subsystem is enabled. You can access them via `remoteParticipants`.

3. **Error Handling**  
   - If a request fails or the server returns an error, the underlying `RoomClient` may throw an exception.
   - Stream operations (`createStream`, `write`, etc.) may also fail if the remote participant rejects or if the connection is lost.

4. **Extensibility**  
   You can add additional features—like chunk-level encryption, compression, or custom handshake logic—by extending `MessagingClient` or by wrapping the provided stream objects (`MessageStreamWriter` / `MessageStreamReader`).

5. **Performance**  
   For large or frequent streams, consider buffering or batching chunk writes in your client logic. The streaming mechanism in `MessagingClient` is designed to handle incremental data transfer, but network considerations remain important for efficient performance.

## Related guides

- [Room API Overview](./overview)
- [Queues API](./queue)
- [Sync API](./sync)
