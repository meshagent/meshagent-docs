---
title: OAuth Credential Secrets
description: OAuth credentials use user secrets and proxy access.
---

OAuth credentials are stored as user-owned secrets. Services and agents use those credentials through the HTTP or MCP proxy when the owning user grants proxy access to the service account.

See [Secrets and Credentials](./overview) for the current model.
