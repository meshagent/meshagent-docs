---
title: "Chatbot"
---
import Main from "/snippets/examples/python/services/chatbot/main.mdx"

If you want to create a chatbot service that will run in every room:
```
meshagent service create --name "chatbot" --image docker.io/meshagent/sample-chatbot:v0.0.31 --env="MESHAGENT_PORT=8090" --port "num=8090 type=meshagent.callable liveness=/ path=/agent participant_name=chatbot"
```

If you want to test the chatbot with a single room:
```
meshagent service test --room my-room --name "chatbot" --image docker.io/meshagent/sample-chatbot:v0.0.31 --env="MESHAGENT_PORT=8090" --port "num=8090 type=meshagent.callable liveness=/ path=/agent participant_name=chatbot"
```

Here is the chatbot service code if you want to create your own app:

<CodeGroup>
    <Main />
</CodeGroup>


