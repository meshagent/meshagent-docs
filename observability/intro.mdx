---
title: "Observability"
description: Understand built-in telemetry, how to enable it with `otel_config`, and where to inspect it in MeshAgent Studio.
---

MeshAgent uses [OpenTelemetry](https://opentelemetry.io/) for traces, logs, and metrics.

Use observability when you want to understand what your agents, tools, and services are doing in a room, how long work takes, where errors happen, and where usage or cost is coming from.

## Enable telemetry in your service

Call `otel_config()` once at startup before you create your service host or room client:

```python Python
from meshagent.otel import otel_config
otel_config(service_name="my-service")
```

When your code runs inside MeshAgent, deployed services and room runtimes receive `OTEL_ENDPOINT`, `MESHAGENT_PROJECT_ID`, `MESHAGENT_ROOM`, and `MESHAGENT_SESSION_ID`. `otel_config()` uses those values to export telemetry with the right project, room, and session tags.

If you call `otel_config()` outside MeshAgent, it still configures logging, but traces and metrics are only exported if you provide an `OTEL_ENDPOINT` yourself.

To change the log level:

```python Python
otel_config(service_name="my-service", level="DEBUG")
```

## What MeshAgent captures

- **Traces** for the full path of a task through your agents and tools
- **Spans** for individual steps such as agent turns, tool calls, or custom work you instrument
- **Metrics** for usage and performance, including latency, tokens, and cost
- **Logs** for contextual debugging information from your service and runtime

## What gets tagged automatically

MeshAgent tags telemetry so it lands in the right room and aggregates cleanly at the project level.

| Tag | Description |
| --- | --- |
| `project` | The current project ID |
| `room` | The current room name |
| `session` | The active session ID |
| `service.name` | The name you pass to `otel_config()` |

## Where you see it

- **Developer Console** in [MeshAgent Studio](../interfaces/meshagent_studio) for live logs, traces, and metrics while you work in a room
- **Session Viewer** for a focused per-session view during or after a room session
- **Usage** in MeshAgent Studio for project-level cost, activity, and latency summaries

## Next Steps
- [Custom Logs, Traces, and Metrics](./custom_otel): add your own spans, logs, and counters when you need deeper visibility
