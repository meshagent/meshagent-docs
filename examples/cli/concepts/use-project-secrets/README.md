# Use Project Secrets

This concept example shows how a project `keys` secret is attached to a CLI-based service manifest.

It is intentionally minimal and is meant to demonstrate secret wiring, not a full third-party integration.
