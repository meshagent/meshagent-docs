# Use Room Secrets

This concept example shows how a room secret is injected into a CLI-based room service through `container.environment[].secret`.

It demonstrates how the secret reaches the service runtime, not a full external integration.
