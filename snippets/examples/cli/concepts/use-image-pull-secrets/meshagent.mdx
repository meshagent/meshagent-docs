```yaml Yaml
version: v1
kind: Service
metadata:
  name: private-image-demo
container:
  run_as:
    email: private-image@service.YOUR_PROJECT_KEY.YOUR_API_DOMAIN
    scopes:
      - secrets:proxy
  image: registry.example.com/my-team/private-app:latest

```
