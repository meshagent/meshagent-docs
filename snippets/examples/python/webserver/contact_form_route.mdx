```python Python
from __future__ import annotations

from aiohttp import web

from meshagent.api.room_server_client import TextDataType

METHODS = ["GET", "POST"]

_FORM_HTML = """<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Contact us</title>
    <style>
      :root {
        color-scheme: light;
        --ink: #1d1f24;
        --muted: #58606b;
        --surface: #ffffff;
        --accent: #0f766e;
        --accent-2: #0b5d56;
        --border: #d8dde6;
        --shadow: 0 18px 40px rgba(26, 31, 41, 0.12);
        --radius: 18px;
        --font: "Instrument Sans", "Space Grotesk", "Segoe UI", sans-serif;
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        font-family: var(--font);
        color: var(--ink);
        background:
          radial-gradient(1200px 600px at 0% 0%, #e0f2f1 0%, transparent 60%),
          radial-gradient(900px 500px at 100% 0%, #e8f0ff 0%, transparent 60%),
          #f6f7fb;
        min-height: 100vh;
        display: grid;
        place-items: center;
        padding: 32px 20px;
      }
      .card {
        width: min(620px, 100%);
        background: var(--surface);
        border-radius: var(--radius);
        box-shadow: var(--shadow);
        border: 1px solid #eef1f6;
        padding: 28px 28px 24px;
      }
      header {
        display: grid;
        gap: 6px;
        margin-bottom: 22px;
      }
      h1 {
        font-size: clamp(26px, 4vw, 34px);
        margin: 0;
        letter-spacing: -0.02em;
      }
      p {
        margin: 0;
        color: var(--muted);
        line-height: 1.5;
      }
      form {
        display: grid;
        gap: 16px;
      }
      label {
        display: grid;
        gap: 8px;
        font-weight: 600;
        font-size: 0.95rem;
      }
      input, textarea {
        width: 100%;
        border: 1px solid var(--border);
        border-radius: 12px;
        padding: 12px 14px;
        font-size: 1rem;
        font-family: var(--font);
        transition: border-color 0.2s ease, box-shadow 0.2s ease;
        background: #fcfdff;
      }
      input:focus, textarea:focus {
        outline: none;
        border-color: var(--accent);
        box-shadow: 0 0 0 4px rgba(15, 118, 110, 0.15);
      }
      textarea { resize: vertical; min-height: 140px; }
      .actions {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 12px;
        margin-top: 6px;
      }
      .hint {
        font-size: 0.9rem;
        color: var(--muted);
      }
      button {
        border: none;
        border-radius: 999px;
        padding: 12px 22px;
        background: linear-gradient(135deg, var(--accent), #0ea5a4);
        color: #fff;
        font-weight: 600;
        font-size: 1rem;
        cursor: pointer;
        box-shadow: 0 10px 20px rgba(15, 118, 110, 0.2);
        transition: transform 0.15s ease, box-shadow 0.2s ease;
      }
      button:hover { transform: translateY(-1px); box-shadow: 0 12px 24px rgba(15, 118, 110, 0.24); }
      button:active { transform: translateY(0); }
      @media (max-width: 520px) {
        .card { padding: 22px; }
        .actions { flex-direction: column; align-items: flex-start; }
      }
    </style>
  </head>
  <body>
    <main class="card">
      <header>
        <h1>Contact Us</h1>
        <p>Send a note and we will route it into your MeshAgent room database.</p>
      </header>
      <form method="post">
        <label>
          Name
          <input name="name" placeholder="Ada Lovelace" required />
        </label>
        <label>
          Email
          <input name="email" type="email" placeholder="ada@example.com" required />
        </label>
        <label>
          Message
          <textarea name="message" placeholder="How can we help?" required></textarea>
        </label>
        <div class="actions">
          <span class="hint">We typically reply within 1 business day.</span>
          <button type="submit">Send message</button>
        </div>
      </form>
    </main>
  </body>
</html>
"""


async def handler(*, room, req: web.Request) -> web.StreamResponse:
    if req.method == "GET":
        return web.Response(text=_FORM_HTML, content_type="text/html")

    form = await req.post()
    await room.database.create_table_with_schema(
        name="contacts",
        schema={
            "name": TextDataType(),
            "email": TextDataType(),
            "message": TextDataType(),
        },
        mode="create_if_not_exists",
        data=None,
    )
    await room.database.insert(
        table="contacts",
        records=[
            {
                "name": form.get("name", "").strip(),
                "email": form.get("email", "").strip(),
                "message": form.get("message", "").strip(),
            }
        ],
    )
    thanks_html = """<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Thank you</title>
    <style>
      :root {
        color-scheme: light;
        --ink: #1d1f24;
        --muted: #58606b;
        --surface: #ffffff;
        --accent: #0f766e;
        --accent-2: #0b5d56;
        --border: #d8dde6;
        --shadow: 0 18px 40px rgba(26, 31, 41, 0.12);
        --radius: 18px;
        --font: "Instrument Sans", "Space Grotesk", "Segoe UI", sans-serif;
      }
      * { box-sizing: border-box; }
      body {
        margin: 0;
        font-family: var(--font);
        color: var(--ink);
        background:
          radial-gradient(1200px 600px at 0% 0%, #e0f2f1 0%, transparent 60%),
          radial-gradient(900px 500px at 100% 0%, #e8f0ff 0%, transparent 60%),
          #f6f7fb;
        min-height: 100vh;
        display: grid;
        place-items: center;
        padding: 32px 20px;
      }
      .card {
        width: min(620px, 100%);
        background: var(--surface);
        border-radius: var(--radius);
        box-shadow: var(--shadow);
        border: 1px solid #eef1f6;
        padding: 28px 28px 24px;
      }
      h1 {
        font-size: clamp(26px, 4vw, 34px);
        margin: 0 0 8px;
        letter-spacing: -0.02em;
      }
      p {
        margin: 0 0 18px;
        color: var(--muted);
        line-height: 1.5;
      }
      a {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        text-decoration: none;
        color: var(--accent-2);
        font-weight: 600;
      }
      .button {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        border-radius: 999px;
        padding: 12px 20px;
        background: linear-gradient(135deg, var(--accent), #0ea5a4);
        color: #fff;
        font-weight: 600;
        text-decoration: none;
        box-shadow: 0 10px 20px rgba(15, 118, 110, 0.2);
        transition: transform 0.15s ease, box-shadow 0.2s ease;
      }
      .button:hover { transform: translateY(-1px); box-shadow: 0 12px 24px rgba(15, 118, 110, 0.24); }
      .button:active { transform: translateY(0); }
      @media (max-width: 520px) {
        .card { padding: 22px; }
      }
    </style>
  </head>
  <body>
    <main class="card">
      <h1>Thanks for reaching out</h1>
      <p>Your message is in the queue. Someone from the team will follow up soon.</p>
      <a class="button" href=".">Send another message</a>
    </main>
  </body>
</html>
"""
    return web.Response(text=thanks_html, content_type="text/html")

```

