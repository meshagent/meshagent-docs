---
title: External Service Hosted Elsewhere
---
import ExternalServiceExample from "/snippets/examples/packaging/external_service.mdx"

Use an external service when the agent runs on infrastructure you manage. MeshAgent routes requests to the URL you provide.

<CodeGroup>
  <ExternalServiceExample />
</CodeGroup>

`external.url` is the base URL, and each endpoint `path` is appended to it.
