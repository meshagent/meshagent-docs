# Use Image Pull Secrets

This concept example shows a service running as a service account. Registry credentials are attached to the service account and used automatically when the service pulls private images.

It is intentionally minimal because the point is registry authentication, not the application runtime inside the container.
