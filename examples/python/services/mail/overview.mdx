---
title: "Mail"
---
import Main from "/snippets/examples/python/services/mail/main.mdx"

If you want to create a mail service that will run in every room:
```
meshagent service create --name "mail" --image docker.io/meshagent/sample-mail:v0.0.31 --env="MESHAGENT_PORT=8090" --port "num=8090 type=meshagent.callable liveness=/ path=/agent participant_name=mail"
```

If you want to test the mail with a single room:
```
meshagent service test --room my-room --name "mail" --image docker.io/meshagent/sample-mail:v0.0.31 --env="MESHAGENT_PORT=8090" --port "num=8090 type=meshagent.callable liveness=/ path=/agent participant_name=mail"
```

Here is the mail service code if you want to create your own app:

<CodeGroup>
    <Main />
</CodeGroup>
